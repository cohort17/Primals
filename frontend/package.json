{
  "name": "primals-frontend",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://cohort17.github.io/Primals/",
  "dependencies": {
    "axios": "^1.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "jest": "^29.6.0",
    "gh-pages": "^6.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "lint": "eslint ./src",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
